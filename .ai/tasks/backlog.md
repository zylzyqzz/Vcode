# Backlog

- Add automatic worktree merge and conflict-resolution nodes.
- Add task graph progress events to the compact interactive TUI.
- Add role-specific tool scopes and richer extension hooks.
- Add distributed/remote worker execution.
