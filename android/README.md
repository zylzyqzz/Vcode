# Vcode Android 自用版

这是一个轻量全屏 WebView 壳，Agent、会话、SSE 和模型配置都运行在云端 `https://v.aimj.xin`。

使用 Android Studio 打开 `android/`，等待 Gradle 同步后运行或生成 APK。首次启动需要登录云端密码。

APK 自用安装即可，不需要发布到应用商店。正式签名时请使用自己的 keystore，不要把 keystore 提交到仓库。
